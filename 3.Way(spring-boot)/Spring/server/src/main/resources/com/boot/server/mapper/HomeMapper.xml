<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.server.mapper.HomeMapper">

    <select id="allLocTop" resultType="Board">
        SELECT * FROM board ORDER BY board_like DESC LIMIT 5
    </select>

</mapper>